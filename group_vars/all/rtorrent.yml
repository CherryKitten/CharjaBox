rtorrent_enabled: false

rtorrent_tag: "latest"
rtorrent_directories:
  config: "{{ docker_home }}/rtorrent/config"
  downloads: "{{ charjabox_downloads_directory }}"

rtorrent_traefik_enabled: false
rtorrent_domain: "rtorrent.{{ charjabox_domain }}"
