lidarr_enabled: false

lidarr_config_directory: "{{ docker_home }}/lidarr/config"
lidarr_data_directory: "{{ docker_home }}/lidarr/data"

lidarr_traefik_enabled: false
lidarr_domain: "lidarr.{{ charjabox_domain }}"
